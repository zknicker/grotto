diff --git a/dist/agent/index.d.ts b/dist/agent/index.d.ts
index dcf8357e79aff9d23eca41dc9c20d18eddd7d8dd..e9106da0616d702a84368778f5e36f44e0a43701 100644
--- a/dist/agent/index.d.ts
+++ b/dist/agent/index.d.ts
@@ -1377,6 +1377,12 @@ declare class HarnessAgentSession {
      * the hood). Throws if the session has ended.
      */
     compact(customInstructions?: string): Promise<void>;
+    /**
+     * Deliver a user message into the turn currently in flight. Resolves true
+     * when the adapter accepted the message; false when no turn is running or
+     * the adapter does not support mid-turn input.
+     */
+    sendUserMessage(text: string): Promise<boolean>;
     /**
      * Park the session, returning a payload the caller can persist and later
      * pass to `agent.createSession({ sessionId, resumeFrom })` to reconnect.
diff --git a/dist/agent/index.js b/dist/agent/index.js
index 342cf72d3124441648639f86f73ada1bf677aea3..23f0a41a3ccc2a441933d30d324fcf099a0dd036 100644
--- a/dist/agent/index.js
+++ b/dist/agent/index.js
@@ -1440,6 +1440,7 @@ function runPrompt(input) {
       return;
     }
     const { stream, control } = bridge;
+    if (input.onTurnControl) input.onTurnControl(control);
     const reader = stream.getReader();
     const toolCallsByToolCallId = /* @__PURE__ */ new Map();
     const rawToolCallsByToolCallId = /* @__PURE__ */ new Map();
@@ -2172,6 +2173,7 @@ var HarnessAgentSession = class {
     this.sessionState = "active";
     this.turnSequence = 0;
     this.activeTurnSequence = 0;
+    this.activeTurnControl = null;
     var _a4, _b4, _c;
     this.sessionId = options.sessionId;
     this.harness = options.harness;
@@ -2253,10 +2255,15 @@ var HarnessAgentSession = class {
         onToolResultSettled: (toolCallId) => {
           this.pendingToolResults.delete(toolCallId);
         },
+        onTurnControl: (control) => {
+          this.activeTurnControl = control;
+        },
         onTurnFinished: () => {
+          this.activeTurnControl = null;
           this.finishTrackedTurn({ turnId });
         },
         onTurnFailed: () => {
+          this.activeTurnControl = null;
           this.finishTrackedTurn({ turnId });
         },
         isTurnSuspending: () => this.activeTurnSequence === turnId && this.suspendedTurnState != null,
@@ -2264,6 +2271,7 @@ var HarnessAgentSession = class {
       });
       return turn;
     } catch (error) {
+      this.activeTurnControl = null;
       this.finishTrackedTurn({ turnId });
       throw error;
     }
@@ -2308,10 +2316,15 @@ var HarnessAgentSession = class {
         onToolResultSettled: (toolCallId) => {
           this.pendingToolResults.delete(toolCallId);
         },
+        onTurnControl: (control) => {
+          this.activeTurnControl = control;
+        },
         onTurnFinished: () => {
+          this.activeTurnControl = null;
           this.finishTrackedTurn({ turnId });
         },
         onTurnFailed: () => {
+          this.activeTurnControl = null;
           this.finishTrackedTurn({ turnId });
         },
         isTurnSuspending: () => this.activeTurnSequence === turnId && this.suspendedTurnState != null,
@@ -2319,6 +2332,7 @@ var HarnessAgentSession = class {
       });
       return turn;
     } catch (error) {
+      this.activeTurnControl = null;
       this.finishTrackedTurn({ turnId });
       throw error;
     }
@@ -2336,6 +2350,22 @@ var HarnessAgentSession = class {
   async compact(customInstructions) {
     await this.requireReusableSession().doCompact(customInstructions);
   }
+  /**
+   * Deliver a user message into the turn currently in flight. Resolves true
+   * when the adapter accepted the message; false when no turn is running or
+   * the adapter does not support mid-turn input.
+   */
+  async sendUserMessage(text) {
+    const control = this.activeTurnControl;
+    if (!control || typeof control.submitUserMessage !== "function") {
+      return false;
+    }
+    if (this.turnState !== "running") {
+      return false;
+    }
+    await control.submitUserMessage(text);
+    return true;
+  }
   /**
    * Park the session, returning a payload the caller can persist and later
    * pass to `agent.createSession({ sessionId, resumeFrom })` to reconnect.
