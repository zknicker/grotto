--- a/dist/index.js
+++ b/dist/index.js
@@ -117,6 +117,7 @@
 var startMessageSchema = harnessV1BridgeStartBaseSchema.extend({
   thinking: z.enum(["off", "on", "adaptive"]).optional(),
   maxTurns: z.number().optional(),
+  disallowedTools: z.array(z.string()).optional(),
   skills: z.array(z.string()).optional(),
   // Resume signal. When true, the bridge passes `{ continue: true }` to the
   // Claude SDK so the in-workdir thread state is rehydrated. The host sets this
@@ -434,6 +435,7 @@
       const lifecycleState = startOpts.continueFrom ?? startOpts.resumeFrom;
       const isResume = lifecycleState != null;
       const isContinue = startOpts.continueFrom != null;
+      const refreshInstructions = lifecycleState?.data?.refreshInstructions === true;
       const coords = isResume ? lifecycleState?.data?.bridge : void 0;
       const workDir = startOpts.sessionWorkDir;
       const sessionDataDir = `${sandboxSession.defaultWorkingDirectory}/.agent-runs/${startOpts.sessionId}`;
@@ -472,7 +474,9 @@
             model: settings.model,
             maxTurns: settings.maxTurns,
             thinking: settings.thinking,
+            disallowedTools: settings.disallowedTools,
             isResume: true,
+            refreshInstructions,
             continueOnFirstPrompt: false,
             rerunContinue: false,
             bridgePort: coords.port,
@@ -590,7 +594,9 @@
         model: settings.model,
         maxTurns: settings.maxTurns,
         thinking: settings.thinking,
+        disallowedTools: settings.disallowedTools,
         isResume: respawnStrategy !== void 0,
+        refreshInstructions,
         continueOnFirstPrompt: respawnStrategy !== void 0,
         rerunContinue: respawnStrategy === "rerun",
         bridgePort: boundPort,
@@ -947,7 +953,9 @@
   model,
   maxTurns,
   thinking,
+  disallowedTools,
   isResume,
+  refreshInstructions,
   continueOnFirstPrompt,
   rerunContinue,
   bridgePort,
@@ -960,7 +968,7 @@
   let stopped = false;
   let stopPromise;
   let pendingResumeFlag = continueOnFirstPrompt;
-  let instructionsApplied = isResume;
+  let instructionsApplied = isResume && !refreshInstructions;
   const wireTurn = (turnOpts) => {
     let pendingResolve;
     let pendingReject;
@@ -1099,6 +1107,7 @@
         model,
         maxTurns,
         thinking,
+        ...disallowedTools && disallowedTools.length > 0 ? { disallowedTools: [...disallowedTools] } : {},
         ...skills.length > 0 ? { skills: skills.map((skill) => skill.name) } : {},
         ...permissionMode ? { permissionMode } : {},
         ...debug ? { debug } : {},
@@ -1133,6 +1142,7 @@
           model,
           maxTurns,
           thinking,
+          ...disallowedTools && disallowedTools.length > 0 ? { disallowedTools: [...disallowedTools] } : {},
           ...skills.length > 0 ? { skills: skills.map((skill) => skill.name) } : {},
           ...permissionMode ? { permissionMode } : {},
           ...debug ? { debug } : {},
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -32,6 +32,11 @@
      */
     readonly thinking?: 'off' | 'on' | 'adaptive';
     /**
+     * Native Claude Code tool names (e.g. `WebSearch`, `WebFetch`) removed
+     * from the runtime's tool set via the SDK `disallowedTools` option.
+     */
+    readonly disallowedTools?: readonly string[];
+    /**
      * Override the port the bridge binds inside the sandbox. By default the
      * adapter uses the first port the sandbox declares via `sandbox.ports`.
      * Only set this if the sandbox declares multiple ports and the first one
--- a/dist/bridge/index.mjs
+++ b/dist/bridge/index.mjs
@@ -658,6 +658,7 @@
       ...start.maxTurns !== void 0 ? { maxTurns: start.maxTurns } : {},
       ...skillsOption ? { skills: skillsOption } : {},
       ...toThinkingConfig(start.thinking) ? { thinking: toThinkingConfig(start.thinking) } : {},
+      ...Array.isArray(start.disallowedTools) && start.disallowedTools.length > 0 ? { disallowedTools: [...start.disallowedTools] } : {},
       includePartialMessages: true,
       // The `PostCompact` hook carries the compaction summary, which the
       // `compact_boundary` system message does not. Latch it for the unified
